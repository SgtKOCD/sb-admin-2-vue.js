import { createApp } from 'vue'
import App from './App.vue'


createApp(App).mount('#app') //Here, it mounts the app.vue file, which is the main component we created with vue, to the relevant html file.
